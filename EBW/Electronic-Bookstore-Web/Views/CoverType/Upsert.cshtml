@model CoverType
@{
    ViewData["Title"] = Model.Id != 0 ? "Cover Type Editing: " + Model.Name : "New Cover Type";
}

<div class="form-location">
    <div></div>
    <form method="post" asp-action="Upsert" enctype="multipart/form-data">
        <input asp-for="Id" hidden />
        <div class="align-content-center" style="display: flex; justify-content: center;">
            <div class="col-10">
                <div class="border p-3 mt-4 row">
                    <div class="col-12 pb-2">
                        <h3 class="text-primary">@(Model.Id != 0 ? "Editing" : "Adding") Cover Type</h3>
                        <hr />
                    </div>
                    <div class="mb-3">
                        <label asp-for="Name"></label>
                        <input asp-for="Name" class="form-control" />
                        <span asp-validation-for="Name" class="text-danger"></span>
                    </div>
                    <div class="col-12 text-center">
                        @if (Model.Id != 0)
                        {
                            <button type="submit" class="btn btn-primary" style="width:150px">Edit</button>
                        }
                        else
                        {
                            <button type="submit" class="btn btn-success" style="width:150px">Create</button>
                        }
                        <a asp-controller="CoverType" asp-action="Index" class="btn btn-danger" style="width:150px">Cancel</a>
                    </div>
                </div>
            </div>
        </div>
    </form><div></div>
</div>

@section Scripts{
    @{
        <partial name="_ValidationScriptsPartial" />
    }
}